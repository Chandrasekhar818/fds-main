apiVersion: apps/v1
kind: Deployment
metadata:
  name: fds-main
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fds-main
  template:
    metadata:
      labels:
        app: fds-main
    spec:
      containers:
        - name: fds-main
          image: ghcr.io/chandrasekhar818/fds-main:90e72cc5e083f9f8783f04f8bbbe1552aaff91a7
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "production"
      imagePullSecrets:
        - name: ghcr-secret
