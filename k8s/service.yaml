apiVersion: v1
kind: Service
metadata:
  name: fds-main-svc
  namespace: default
spec:
  selector:
    app: fds-main
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 31000
  type: NodePort
